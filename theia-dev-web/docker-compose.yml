version: '3.7'

services: 
    theia:
        build: 
            context: .
        image: theia
        ports: 
            - 3000:3000
        volumes: 
            - ~/.ssh:/home/theia/.ssh/:ro
            - ~/.gitconfig:/home/theia/.gitconfig:ro
            - workspace:/home/workspace

volumes:
    workspace: